আমাদের Illdoor (পলিটেকনিক বুক এক্সচেঞ্জ) প্রজেক্টে এ পর্যন্ত যা যা কমপ্লিট করা হয়েছে, তার একটি পূর্ণাঙ্গ তালিকা নিচে দেওয়া হলো:
১. ব্র্যান্ডিং ও অ্যানিমেটেড লোগো (ILLDOOR Animated Logo)
কাস্টম অ্যানিমেটেড রিঅ্যাক্ট কম্পোনেন্ট: আপনার দেওয়া SVG এবং অ্যানিমেশন কি-ফ্রেম দিয়ে তৈরি লোগো ইন্টিগ্রেট করা হয়েছে।
সুইংগিং ডোর ও টাইপোগ্রাফি ইফেক্ট: ব্র্যান্ড অরেঞ্জ গ্র্যাডিয়েন্ট (#ef4d23 → #ff7a45) সহ 'D' অক্ষরের ডোর লিফটি ওপেন/ক্লোজ হয় এবং টায়ার বল রোলিং হয়ে I, L, L, O, O, R অক্ষরে রূপান্তরের অ্যানিমেশন চলে।
গুগল ফন্ট: Baloo 2 (700 ও 800 ওয়েট) যুক্ত করা হয়েছে।
স্মার্ট সাইজিং ও সিঙ্গেল ইনস্ট্যান্স: স্ক্রিন সাইজ অনুযায়ী লোগো নিজে থেকেই স্কেল হয় (ডেস্কটপে 27px, মোবাইলে 21px) এবং ডুপ্লিকেট না হয়ে নিখুঁতভাবে প্রদর্শিত হয়।
২. ডুয়াল ন্যাভিগেশন সিস্টেম (Navigation)
টপ ফ্লোটিং ন্যাভবার (Top Floating Pill):
ডেক্সটপ ও মোবাইলের জন্য অপ্টিমাইজড ব্যালান্সড লেআউট।
লোগো, পেজ লিংকস (Home, Browse, Orders, Pages ড্রপডাউন), উইশলিস্ট আইকন, "Sell a book" অ্যাকশন বাটন এবং প্রোফাইল অবতার।
মোবাইলে সম্পূর্ণ রেসপনসিভ ও স্মুথ ড্রপডাউন মেনু।
বটম ফ্রস্টেড গ্লাস ন্যাভবার (Bottom Glass Dock):
ব্যবহারকারী পেজ স্ক্রোল করে একটু নিচে নামলেই স্বয়ংক্রিয়ভাবে নিচ থেকে স্মুথ অ্যানিমেশনে ভেসে ওঠে।
হাই-এন্ড ট্রান্সপারেন্ট গ্লাস ফিনিশ (bg-white/45, backdrop-blur-2xl, সফট ইনসেট লাইট শাইন)।
আপনার চাওয়া ৩টি কুইক অপশন: Books, Sell Now (CTA) এবং Profile।
৩. হোমপেজ (Home & Explore)
ইন্টারেক্টিভ হিরো সেকশন: ডিপ্লোমা শিক্ষার্থীদের জন্য ডিপার্টমেন্ট ও সেমিস্টার ভিত্তিক দ্রুত বই খোঁজার সার্চবার।
ডিপার্টমেন্ট ও সেমিস্টার ফিল্টার: Computer, Civil, Electrical, Mechanical, Electronics ইত্যাদি টেকনোলজি অনুযায়ী এক ক্লিকে ফিল্টার করার সুবিধা।
ট্রাস্ট ও সেফটি ফিচারস: ক্যাম্পাস পিকআপ পয়েন্ট, এসক্রো পেমেন্ট নিরাপত্তা ও ভেরিফাইড স্টুডেন্ট ব্যাজের হাইলাইটস।
ফিচার্ড ও রিসেন্ট লিস্টিংস: লাইভ কার্ড গ্রিডে বইয়ের কন্ডিশন, ডিসকাউন্ট ও দামের উপস্থাপনা।
৪. বুক ব্রাউজিং ও অ্যাডভান্সড ফিল্টারিং (Browse Page)
বইয়ের নাম, লেখক বা সাবজেক্ট কোড দিয়ে রিয়েল-টাইম সার্চ।
টেকনোলজি, সেমিস্টার (১ম - ৮ম), বইয়ের কন্ডিশন (Like New, Good, Used) এবং প্রাইস রেঞ্জ অনুযায়ী ফিল্টারিং।
সর্টিং অপশন (নতুন লিস্টিং, সর্বনিম্ন দাম, সর্বোচ্চ ছাড়)।
৫. বই বিক্রির পেজ (Sell Book Page)
ডিপ্লোমা ইন ইঞ্জিনিয়ারিং সিলেবাসের সাবজেক্ট কোড ও সেমিস্টার অনুযায়ী তথ্য দেওয়ার ফর্ম।
বইয়ের আসল দাম ও বিক্রয়মূল্য দিলে স্বয়ংক্রিয় সেভিংস ও পার্সেন্টেজ ক্যালকুলেশন।
একাধিক ছবি প্রিভিউ ও ক্যাম্পাস পিকআপ পয়েন্ট সিলেক্ট করার অপশন।
৬. বিস্তারিত বইয়ের পেজ ও বুকিং (Book Details)
বইয়ের বড় ফটো গ্যালারি ও কন্ডিশন ডিটেইলস।
বিক্রেতার ভেরিফাইড প্রোফাইল, রেটিং ও যোগাযোগের তথ্য।
ক্যাম্পাসের ড্রপ-অফ ও পিকআপ পয়েন্টের ঠিকানা এবং সময়সূচি।
"Buy Now" বা সরাসরি অর্ডারের নিরাপদ চেকআউট ফ্লো।
৭. অর্ডার ট্র্যাকিং ও টাইমলাইন (Orders Page)
প্রতিটি অর্ডারের ৬-স্টেপ ভিজ্যুয়াল প্রোগ্রেস টাইমলাইন (Placed ➔ Confirmed ➔ Dropped Off ➔ Ready for Pickup ➔ Completed)।
পিকআপ ভেরিফিকেশন পিন (Verification PIN) এবং এসক্রো মানি সিকিউরিটি সিস্টেম।
৮. প্রোফাইল, উইশলিস্ট ও নোটিফিকেশন
User Profile: ইউজারের তথ্য, ডিপার্টমেন্ট, রোল, মোট বিক্রয় ও ক্রয়ের পরিসংখ্যান।
Wishlist: পছন্দের বই সেভ করে রাখার পেজ।
Notifications Center: অর্ডার আপডেট ও প্ল্যাটফর্ম নোটিফিকেশন দেখার ব্যবস্থা।
Admin Dashboard: লিস্টিং অ্যাপ্রুভাল ও ডিসপিউট পর্যবেক্ষণের আলাদা ড্যাশবোর্ড।
প্রজেক্টের মূল আর্কিটেকচার, ডিজাইন এবং ডেটা-ফ্লো এখন সম্পূর্ণ প্রস্তুত। আপনি কি
প্রজেক্টটিকে ১০০% ফাইনাল ডেপ্লয় রেডি করতে যে যে মূল বিষয়গুলো বাকি আছে তার বিস্তারিত তালিকা:
১. Authentication System (Login & Registration)
বর্তমানে অ্যাপটিতে কোনো লগইন বা সাইন-আপ নেই (ডিফল্টভাবে একটি ডামি প্রোফাইল লোড হয়ে থাকে)। রিয়েল ইউজারদের জন্য যা প্রয়োজন:
Student Registration (সাইন-আপ):
ফুল নেম, পলিটেকনিক রোল ও রেজিস্ট্রেশন নম্বর
ডিপার্টমেন্ট (যেমন: Computer, Civil, Electrical ইত্যাদি)
সেমিস্টার এবং ইনস্টিটিউটের নাম (যেমন: Dhaka Polytechnic Institute)
মোবাইল নম্বর / ইমেইল এবং পাসওয়ার্ড
Login Option:
ইমেইল/রোল ও পাসওয়ার্ড দিয়ে সাইন-ইন
Forgot password / Password reset ফ্লো
Auth Modal / Dedicated Page:
হেডার বা অ্যাকশন বাটনে (যেমন: Buy, Sell, Wishlist) ক্লিক করলে আনঅথোরাইজড ইউজারদের জন্য সুন্দর লগইন/রেজিস্ট্রেশন পপআপ বা পেজ।
Student ID Verification (Badge):
স্টুডেন্ট আইডি কার্ড আপলোড করে "Verified Student" স্ট্যাটাস দেওয়া।
২. Database Connection (যেমন: Supabase)
বর্তমানে একজন ইউজার কোনো বই লিস্ট করলে বা কিনলে তা শুধু তার নিজের ব্রাউজারের ক্যাশেই জমা হয়, অন্য কোনো ইউজার তা দেখতে পায় না। সুপাবেস (বা ক্লাউড ডেটাবেস) কানেক্ট করে নিচের টেবিলগুলো দরকার:
profiles টেবিল: ইউজারের রোল, ডিপার্টমেন্ট, মোবাইল নম্বর, ভেরিফিকেশন স্ট্যাটাস।
books টেবিল: বইয়ের নাম, অথর, সেমিস্টার, বিটিইবি সাবজেক্ট কোড, অরিজিনাল ও সেলিং প্রাইস, কন্ডিশন, পিকআপ পয়েন্ট ও স্ট্যাটাস (Available / Reserved / Sold)।
orders টেবিল: কোন বায়ার কোন সেলারের বই অর্ডার করেছে, ৪-ডিজিট সিক্রেট পিকআপ পিন (PIN), এবং ডেলিভারি স্ট্যাটাস।
notifications ও disputes টেবিল: অর্ডার আপডেট ও কমপ্লেইন ম্যানেজমেন্ট।
৩. Image Upload Storage (Supabase Storage Bucket)
বর্তমানে বই বিক্রির পেজে ছবি তুললে বা আপলোড করলে তা লোকাল মেমোরিতে থাকে।
বইয়ের কভার ও পেজের ছবির জন্য Supabase Storage-এ book-covers নামের একটি পাবলিক বালতিতে ছবি আপলোড করার ফ্লো দরকার।
৪. Realtime Updates & Row-Level Security (RLS)
RLS Policies: যাতে একজন সেলার অন্য কারো বই ডিলিট বা এডিট করতে না পারে। শুধু নিজের বই এডিট করতে পারবে।
PIN Protection: পিকআপ বুথের ৪-ডিজিটের সিক্রেট পিন শুধু বায়ার ও বুথ এজেন্ট দেখতে পাবে, যাতে কোনো স্ক্যাম না হয়।
৫. Deploy & Environment Configuration
Environment Variables (.env.example):
VITE_SUPABASE_URL
VITE_SUPABASE_ANON_KEY
SPA Routing Fallback:
Vercel/Netlify বা Cloud Run-এ ডিপ্লয় করলে রিফ্রেশ দিলে যেন 404 না আসে সেজন্য vercel.json বা রিডাইরেক্ট রুল (বিল্ড কিন্তু অলরেডি npm run build গ্রিন এবং সাকসেসফুল)।

---

# ✅ P0 স্প্রিন্ট — MVP ফাইনাল (সম্পন্ন)

PRD-এর সাথে মিলিয়ে যে চারটি বড় ফাঁক ছিল, সেগুলো এখন ডেটাবেস লেভেল থেকে UI পর্যন্ত সম্পূর্ণ করা হয়েছে।

## ১. Admin Role ও Ops Console Guard (PRD §32–34, §37)

- `profiles.is_admin` কলাম যোগ করা হয়েছে — ক্লায়েন্ট এই ফ্ল্যাগ **পড়তে পারে, লিখতে পারে না**।
- `public.is_admin()` নামে একটি `SECURITY DEFINER` হেল্পার ফাংশন তৈরি, যা RLS পলিসির ভেতরে ব্যবহার হয় (তাই `profiles` টেবিলেই রিকার্সন হয় না)।
- Admin-only RLS পলিসি: যেকোনো listing মডারেট, সব order দেখা/আপডেট, সব dispute দেখা ও নিষ্পত্তি, সব verification request দেখা ও সিদ্ধান্ত।
- **নিরাপত্তার ফাঁক বন্ধ:** আগে যেকোনো লগইন করা ইউজার `/admin` খুলতে পারত। এখন Navbar-এ "Ops Console" শুধু admin-কে দেখা যায়, এবং ভিউ লেভেলে গার্ড বসানো হয়েছে — বাকিরা "Campus Desk Access Only" স্ক্রিন দেখে।
- Admin-এর "Resolve" অ্যাকশন এখন ডেটাবেসে সেভ হয় (আগে শুধু লোকাল স্টেটে ছিল, রিফ্রেশে হারিয়ে যেত)।
- Admin বানানোর নিয়ম (একবার ম্যানুয়ালি):
  `UPDATE public.profiles SET is_admin = TRUE WHERE email = 'admin@yourdomain.com';`

## ২. Peer Reviews ও Trust Score (PRD §26–27)

- নতুন `reviews` টেবিল — প্রতি order-এ প্রতি পক্ষের জন্য একটিমাত্র রিভিউ (`UNIQUE (order_id, reviewer_id)`)।
- `validate_review()` ট্রিগার রিভিউ সেভ করার আগে যাচাই করে: order অবশ্যই `completed`, রিভিউয়ার অবশ্যই সেই order-এর buyer/seller, এবং প্রাপক (reviewee) সার্ভার নিজেই বের করে — ক্লায়েন্টের পাঠানো আইডি বিশ্বাস করা হয় না।
- `refresh_profile_rating()` ট্রিগার `profiles.rating`-কে সব প্রাপ্ত রেটিংয়ের গড় হিসাবে আপডেট রাখে।
- UI: completed order-এ ৫-স্টার রেটিং + কমেন্ট ফর্ম (একবার রিভিউ দিলে লক হয়ে যায়), প্রোফাইলে আসল রিভিউ লিস্ট + **Transaction Verified** ব্যাজ। আগের মক `INITIAL_REVIEWS` আর ব্যবহার হয় না।

## ৩. Student ID Verification — Admin Approval সহ (PRD §34)

- নতুন `verification_requests` টেবিল (roll, registration no, ID কার্ডের path, status, admin note)।
- ব্যক্তিগত `student-ids` স্টোরেজ বাকেট: শুধু মালিক ও admin পড়তে পারে; admin-এর জন্য সাময়িক signed URL দিয়ে ছবি খোলার ব্যবস্থা।
- নতুন trigger নিয়ম — **শুধু admin-এর সিদ্ধান্তেই** `is_verified` সত্য হয়। ছাত্র নিজে নিজেকে verified বানাতে চাইলেও ট্রিগার সেটা আটকে দেয় (আগে মডাল শুধু লোকাল স্টেট বদলাতো, রিফ্রেশে হারিয়ে যেত)।
- Approve/Reject করলে ছাত্র স্বয়ংক্রিয়ভাবে নোটিফিকেশন পায়; reject করার সময় নোটে কারণ লেখা যায়।
- UI: Profile → মডালে BTEB roll, registration no, ID কার্ডের ছবি (কম্প্রেস করে আপলোড), স্ট্যাটাস ব্যানার (Pending / Approved / Rejected + admin note)। Ops Console → নতুন **Student Verification** ট্যাবে রিভিউ কিউ, ছবি দেখা, Approve/Reject।

## 4. Order Cancel Flow ও Status Guard (PRD §14, §18)

- ``cancelOrder()`` — book হ্যান্ডওভারের আগে (placed / confirmed) buyer বা seller যেকোনো পক্ষ অর্ডার বাতিল করতে পারে; কারণ নির্বাচন করে দিতে হয়।
- ডেটাবেস ট্রিগার তখন escrow ফেরত দেয় (payment_state → Refunded), লিস্টিং আবার Available করে এবং দুই পক্ষকেই নোটিফাই করে।
- নতুন ``guard_order_transition`` ট্রিগার — অর্ডার শুধু সামনের দিকেই এগোতে পারে (placed → confirmed → dropped_off → ready_for_pickup → picked_up → completed); completed/cancelled আর বদলানো যায় না। ফলে ক্লায়েন্ট খেয়ালখুশিমতো অর্ডার সোজা completed করে দিতে পারবে না।
- UI: Orders পেজে Cancel Order বাটন + কনফার্মেশন মডাল, এবং বাতিল অর্ডারে কারণ দেখানো হয়।

## 5. সাথে যা ঠিক হলো

- Realtime এখন reviews ও verification queue-ও লাইভ সিঙ্ক করে (আগে শুধু books/orders/notifications)।
- পুরো ``schema.sql`` এখন সত্যিকারভাবে idempotent — প্রতিটি policy ``DROP POLICY IF EXISTS`` দিয়ে রি-ক্রিয়েট হয়, তাই ফাইলটা বারবার চালানো নিরাপদ।
- পুরনো ডেটাবেসের জন্য আলাদা migration ব্লক (``ADD COLUMN IF NOT EXISTS``) যোগ করা হয়েছে।
- README-তে admin bootstrap, review, verification ও cancel ফ্লোর ডকুমেন্টেশন যোগ হয়েছে।

> সতর্কতা: একবার Supabase SQL Editor-এ ``schema.sql`` আবার চালাতে হবে (এখন idempotent, নিরাপদ) — তারপর admin ফ্ল্যাগ সেট করে Ops Console ব্যবহার করা যাবে।

## পরের ধাপ (বাকি কাজ)

P1 — PRD Phase 3: payment/payout টেবিল, platform fee (admin-configurable), evidence সহ report, pickup_events (staff drop-off ফ্লো), reputation stats (cancellation rate, drop-off completion), fake order protection (user_blocks), Book Requests।

P2 — Reference data ও expansion: institutes/departments/semesters/subjects টেবিল (BTEB সাবজেক্ট কোড সহ), institute-ভিত্তিক marketplace scoping, admin সাব-পেজ, /orders/[id] ডিপ-লিংক, Terms ও Privacy পেজ।

P3 — সিদ্ধান্ত দরকার: PRD-তে স্ট্যাক Next.js লেখা, কিন্তু বাস্তবায়ন Vite SPA — Next.js-এ মাইগ্রেট করব নাকি Vite-ই রাখব; আর pickup PIN 4-ডিজিট থাকবে নাকি PRD অনুযায়ী 6-ডিজিট হবে।

## 6. ডিপ্লয় ও ইনফ্রা প্রস্তুতি (P0-এর আগে সম্পন্ন)

- ``.env.example``-এ ``VITE_SUPABASE_URL`` / ``VITE_SUPABASE_ANON_KEY`` যোগ (ডকুমেন্টেশন সহ)।
- SPA রিফ্রেশ ফলব্যাক: ``vercel.json`` (rewrite + asset caching), ``netlify.toml``, ``public/_redirects`` — ডিপ লিংক/রিফ্রেশে 404 আসবে না।
- প্রথম ধাপের Realtime (books/orders/notifications) ও ``orders_lifecycle`` ট্রিগার (escrow release, stats, notifications)।
- ``README.md`` সম্পূর্ণ নতুন করে লেখা: সেটআপ, ডেটাবেস সেটআপ, রোল/রিভিউ/ভেরিফিকেশন, ডিপ্লয় ও ট্রাবলশুটিং।
- প্রোডাকশন বিল্ড যাচাই: lint + build গ্রিন, Supabase কনফিগ baked-in, ``dist`` ডিপ্লয়-রেডি (0.87 MB)।

## 7. ডেটাবেস আপগ্রেড প্যাচ — ``schema-p0-upgrade.sql`` (নতুন)

ব্যবহারকারীর চলমান ডেটাবেসে পুরনো স্কিমা থাকায় ``is_admin`` এরর এসেছিল; তাই পুরো ``schema.sql`` না চালিয়ে শুধু P0-র নতুন অংশগুলোর জন্য আলাদা প্যাচ ফাইল তৈরি:

- নতুন কলাম + ``reviews`` / ``verification_requests`` টেবিল + সব admin RLS পলিসি + ৬টি ফাংশন/ট্রিগার + ``student-ids`` প্রাইভেট বাকেট + realtime publication — সব এক ফাইলে, সব idempotent।
- ফাইলের শেষে ব্যবহারকারীর ইমেইল দিয়ে admin grant + ভেরিফিকেশন SELECT আছে।
- README-তে "Upgrading an existing database?" নির্দেশনা যোগ করা হয়েছে।
