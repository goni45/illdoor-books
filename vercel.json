{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix \"better UI,Ux\" install && npm --prefix \"better UI,Ux\" run build",
  "outputDirectory": "better UI,Ux/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
